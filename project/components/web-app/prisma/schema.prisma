generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model EaDrawing {
  id       Int     @id @default(autoincrement())
  filename String?
  version  Int?
  drawings Json?
  user_id  Int?

  @@map("ea-drawings")
}
